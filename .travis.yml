language: cpp
matrix:
  include:
    - compiler: gcc
      os: linux
    - compiler: clang
      os: osx

addons:
  apt:
    packages:
    - gcc-5.1
    - g++-5.1
    - clang

script:
  - mkdir build && cd build
  - cmake ..
  - make

branches:
  only:
    - master

notifications:
  email: false
